{
  "@type": "type.googleapis.com/google.cloud.connectors.v1.RuntimeEntitySchema",
  "entity": "LaborMappingRules_Metric-Based_Weighted_Rules",
  "fields": [
    {
      "dataType": "DATA_TYPE_VARCHAR",
      "description": "The unique identifier type. Each \"ID\" for an instance of an object contains a type and a value. A single instance of an object can have multiple \"ID\" but only a single \"ID\" per \"type\".  Some \"types\" require a reference to a parent instance.",
      "field": "LaborMappingRule_Scheduling_Subgroup_Organization_Reference_WID",
      "nullable": true
    },
    {
      "dataType": "DATA_TYPE_VARCHAR",
      "description": "A weighted rule line for a Metric-Based Rule consisting of a weight, metric, and tier lines.",
      "field": "Metric-Based_Weighted_Rule_Lines",
      "nullable": true
    }
  ],
  "jsonSchema": {
    "properties": {
      "LaborMappingRule_Scheduling_Subgroup_Organization_Reference_WID": {
        "description": "The unique identifier type. Each \"ID\" for an instance of an object contains a type and a value. A single instance of an object can have multiple \"ID\" but only a single \"ID\" per \"type\".  Some \"types\" require a reference to a parent instance.",
        "type": [
          "string",
          "null"
        ]
      },
      "Metric-Based_Weighted_Rule_Lines": {
        "description": "A weighted rule line for a Metric-Based Rule consisting of a weight, metric, and tier lines.",
        "type": [
          "string",
          "null"
        ]
      }
    },
    "type": [
      "object"
    ]
  },
  "operations": [
    "GET",
    "LIST"
  ]
}